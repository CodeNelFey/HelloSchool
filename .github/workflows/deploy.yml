name: HelloSchool Deployement

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Download project form git
        uses: actions/checkout@v2

      - name: Build project
        run: |
          echo("hello world")